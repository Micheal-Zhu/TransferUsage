<Window x:Class="BalanceDock.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="连接站点"
        Width="980" Height="720" MinWidth="760" MinHeight="560"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="CanResizeWithGrip"
        AllowsTransparency="False" Background="Transparent"
        ShowInTaskbar="True">
    <Border Background="{DynamicResource SurfaceStrongBrush}" BorderBrush="{DynamicResource HairlineBrush}" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="{DynamicResource SurfaceBrush}" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Margin="18,0,0,0">
                    <TextBlock x:Name="TitleText" FontSize="15" FontWeight="SemiBold" />
                    <TextBlock x:Name="StatusText" Text="等待登录…" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0" />
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" ToolTip="关闭" Click="Close_Click" Margin="0,6,6,6">
                    <Path Data="M 8,8 L 20,20 M 20,8 L 8,20" Stroke="{DynamicResource TextPrimaryBrush}" StrokeThickness="1.8" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                </Button>
            </Grid>
            <Border Grid.Row="1" BorderBrush="{DynamicResource HairlineBrush}" BorderThickness="0,1,0,0" ClipToBounds="True">
                <wv2:WebView2 x:Name="LoginBrowser" />
            </Border>
        </Grid>
    </Border>
</Window>
