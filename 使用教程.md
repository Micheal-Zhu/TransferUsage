# Balance Dock 使用教程

## 一、文件说明

发布目录中的 `BalanceDock-Portable.exe` 是单文件启动器，双击即可运行。它会在第一次启动时把程序组件解压到当前 Windows 用户的本地运行目录，然后打开悬浮余额窗口。

发布文件中没有打包个人站点链接、登录 Cookie、WebView2 浏览器缓存或其他用户数据。发布包只包含程序本身和运行所需的 WebView2 .NET 组件。

## 二、首次使用

1. 双击 `BalanceDock-Portable.exe`。
2. 点击窗口右上角的“+”按钮。
3. 输入站点首页、控制台或余额页面地址，例如 `https://你的站点.example`。
4. 在弹出的站点窗口中完成登录。
5. 登录成功并检测到余额后，站点会自动加入悬浮列表。

账号密码不会由 Balance Dock 保存。登录会话由 WebView2 的本地用户配置保存，因此下次启动通常无需重复登录。

## 三、日常操作

- 点击站点卡片可刷新余额。
- 点击站点卡片右侧的菜单可编辑名称、货币显示、删除站点或查看用量。
- 关闭窗口会收进系统托盘，不会退出程序。
- 在系统托盘图标上点击右键，可以切换“始终置顶”、开机启动和退出程序。

## 四、数据位置与迁移

发布版的站点配置保存在独立目录：

```text
%LOCALAPPDATA%\BalanceDock-Portable\stations.json
```

窗口设置保存在同一目录下的 `settings.json`，登录会话和浏览器缓存位于 `WebView2` 子目录。原开发版或其他版本使用的 `%LOCALAPPDATA%\BalanceDock` 目录不会被发布版读取、修改或删除。

需要迁移发布版个人配置时，退出程序后复制整个 `%LOCALAPPDATA%\BalanceDock-Portable` 目录即可。

## 五、常见问题

### 双击后没有显示窗口

请确认使用的是 64 位 Windows，并检查系统是否安装了 Microsoft Edge WebView2 Runtime。Windows 11 通常已预装；如果提示缺少 WebView2，请安装微软官方的 Evergreen WebView2 Runtime 后再启动。

### 余额显示为空或提示未登录

重新打开站点卡片完成登录，然后点击刷新。站点必须允许在浏览器中访问其余额接口；部分站点还需要先打开控制台页面。

### 想清空当前配置

退出 Balance Dock 后，删除 `%LOCALAPPDATA%\BalanceDock` 目录即可。下次启动会以空白配置开始。删除前请确认不再需要其中的站点配置和登录会话。

## 六、版本信息

本发布包面向 Windows x64，程序版本为 1.1.0。发布包的 SHA-256 校验值以打包命令输出为准。
